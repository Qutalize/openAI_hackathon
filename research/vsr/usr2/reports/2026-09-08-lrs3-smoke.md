# USR 2.0 Base+：英語VSR試運転

2026-09-08、Codex実施。**環境構築、重みロード、動画1本から英語テキストへの推論に成功。**
上流コードの修正は不要だった。入力言語・出力言語はいずれも英語。

## 実行結果

`modality=v`、CPU、MediaPipe、Base+ high-resource、beam size 40、CTC weight 0.1で実行。
モデルの生の文章出力は以下。独自の文章補正・翻訳・音素列からの復元は加えていない。

```text
BOTH OF THEM BRING THE IMAGINATION TO LIFE IN WAYS THAT REMIND US THAT ALL OF OUR PONDS IN THE END
```

- 実行成否：終了コード0。145フレームの口領域生成、strictな重みロード、映像encoder、beam searchまで完了。
- 認識内容の正しさ：正解文が未確認のため未評価。WER/CERは未測定。
- 主観的所感：文章まで得られ、単体VSRの動作確認はできた。ただし文章末尾を含む正否は判断できず、これだけで採用や精度の優劣は決めない。
- 信頼度：上流demoは確率値を提供しない。beam searchのスコアを確率として扱っていない。
- 壁時計：**34.01秒**。プロセス起動・import・動画読込・前処理・重みロード・推論・出力を含む1回。
- 最大RSS：**6,400,800 KiB（約6.10 GiB）**。CPUスレッド設定8。
- 前処理/encode/decodeの独立した単調時計計測、warm試行、p50/p95、GPU/VRAMは未測定。
- 重みのSHA256計算を別プロセスで同時実行しており、厳密な性能ベンチマークではない。ストレージキャッシュも制御していない。

## 再現コマンドと生ログ

作業ディレクトリ：`research/vsr/usr2/`。

```bash
bash scripts/run_vsr.sh \
  outputs/20260908-lrs3-smoke/input/NqOjj1FCcVY_00007_gt_silent.mp4 \
  outputs/20260908-lrs3-smoke/run
```

これは実行済みコマンドに対応する。再実行時は出力ディレクトリ名を変更する。

生ログは`outputs/20260908-lrs3-smoke/run/console.log`、上流ログは`demo.log`、
GNU timeの計測は`time.txt`、実設定は`.hydra/config.yaml`と`.hydra/overrides.yaml`。
口領域映像は`mouth_crop.mp4`。環境一覧は`outputs/20260908-lrs3-smoke/environment.txt`。

## 上流とモデル

- URL：<https://github.com/ahaliassos/usr2>
- revision：`df0c78b7a3807e625a0fcdadd14b1cf674d21c91`
- 作業場所：`upstream/`。実行後の`git status --porcelain`は空で、上流コードの変更なし。
- 選定：READMEで軽量な選択肢とされるfine-tuned Base+ high-resource（LRS3+Vox2）。文章認識用のfull modelを使用し、encoder-only重みは選ばなかった。
- 配布元：<https://drive.google.com/file/d/18vmJjdem5XPOA8bmizybIW5sLJuHMdRR/view>
- 保存先：`weights/usr2_baseplus_high_resource.pth`
- 実サイズ：1,389,096,590 bytes（約1.39 GB / 1.29 GiB）。Huge/Largeは取得していない。
- SHA256：`4ab1f3f230aa6a724bcc698db8a17f2f54919136d2885c1a07a6b69247012773`
- backbone：`resnet_transformer_baseplus`。上流既定はHugeのため明示的に指定。
- 顔検出器・平均顔・token一覧のSHA256：`artifacts.sha256`を参照。
- コードのLICENSE：CC BY-NC 4.0。重み固有の追加条件は未確認。試験動画の再配布条件も未確認で、ローカル検証のみに使用。
- 重みは推論のためにロードし、巨大なテンソルやバイナリの内容は表示していない。

## 環境と導入時の対応

- OS：Linux 6.8.0-137-generic、CPU：Intel Core i7-14700K、搭載RAM：約62 GiB。
- `nvidia-smi`はドライバと通信できず失敗。`torch.cuda.is_available()`はFalse。
- uv 0.12.10、Python 3.10.21。conda不使用。
- 専用環境：`~/.virtualenvs/usr2-vsr`。作業場所の`.venv`はそこへのsymlink。
- PyTorch 2.5.1+cpu、torchvision 0.20.1+cpu、torchaudio 2.5.1+cpu。
- MediaPipe 0.10.21、NumPy 1.26.4、OpenCV 4.11.0.86、PyAV 13.1.0、FFmpeg 6.1.1。
- 全68パッケージの固定バージョン：`requirements-uv.lock`。`uv pip check`は整合性エラーなし。
- 共有NFS上へのuvインストールはwheelの`RECORD`検証がPermission deniedで失敗。ローカルディスクへ移して解消。失敗した環境は`outputs/failed-nfs-venv/`に退避した。
- ネットワーク制限による名前解決失敗後、承認された制限外コマンドでclone・依存取得・重み取得を行った。
- MediaPipeはEGL/GPU利用不可のログを出すが、XNNPACKによるCPU処理で完了した。
- `bash -n`による補助スクリプトの構文確認、主要依存のimport、Hydra設定読み出し、MediaPipe初期化、実動画推論を確認した。

## 入力と前処理

入力：`NqOjj1FCcVY_00007_gt_silent.mp4`。
SHA256：`d83611a0d7fec6107c41a46551bbe0287911d30e5d4a8e1a0abb8c91fb2e3978`。

既存の[VALLR試運転記録](../../vallr/VALLR/reports/2026-09-08-lrs3-smoke.md)で使用した
LipVoicer公開デモ由来のLRS3サンプルをコピー。VALLRと同一入力ハッシュ。
ffprobeで224×224、25 fps、145フレーム、5.8秒、映像ストリームのみを確認した。
撮影条件・話者・正解文の追加確認はしていない。

上流のMediaPipeで顔ランドマークを検出し、平均顔へのアフィン整列と96×96の口領域cropを行う。
88×88の中央crop、グレースケール化、255で除算、平均0.421・標準偏差0.165で正規化。
全145フレームを使用。25 fpsへの再サンプリングは入力が25 fpsのため不要だった。
上流は音声のない入力に無音テンソルを作るが、`modality=v`の分岐は`model.encoder(xs_v=video_input)`のみを呼ぶ。
音声を認識には使っていない。

VALLRは16フレームを使用し音素列まで、USR2は145フレームを使用し文章までという違いがある。
同一動画でも前処理・出力単位・環境が異なり、今回の実行時間をモデル性能の順位として扱わない。

## 残課題

正解文付きの英語動画による認識精度、同意済みの自前無音口パク、GPU推論、warm時の速度は未確認。
判断は「単体推論成功、採用判定は保留」。Codexが環境定義、実行補助、試し実行と記録を作成した。
第三者による再現確認は未実施。
