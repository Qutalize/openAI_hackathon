# 試運転記録：VALLR / 20260908-lrs3-smoke

## 最初の1本

- 日付・担当：2026-09-08 / Codex
- 状態：推論成功（映像から未後処理の音素クラス列まで）。文章復元は未実装・未確認。
- 上流URL・commit/revision：<https://github.com/MarshallT-99/VALLR> / `9793489136bc00293242a5410def05ba2806e16f`
- 重み/付随ファイルの取得先・SHA256：上流READMEのGoogle Driveリンク。`checkpoints/VALLR.path`、SHA256 `967667d61e705b0bc78d40a3ec80dd7ec449aa1d4d90a5e83c0d4289309ef374`。重みの内容は表示・解析していない。
- コード・重み・データの利用条件：上流READMEはCC BY-NC 4.0を表示。入力はLipVoicerデモページが配布するLRS3サンプルZIPから取得。サンプル動画自体の再配布条件は未確認のため、ローカル試験にのみ使用しGit管理しない。
- OS・CPU・GPU/VRAM・Python・CUDA・依存定義：Linux 6.8.0-137-generic、Intel Core i7-14700K、GPUドライバ利用不可、VRAM未確認、Python 3.10.21、PyTorch 2.4.1+cu121、CUDAビルド12.1、`torch.cuda.is_available() == False`。`requirements-uv.lock`を使用。
- 入力ID・ハッシュ・入力言語・録画条件・使用許可の管理先：`NqOjj1FCcVY_00007_gt_silent.mp4`、SHA256 `d83611a0d7fec6107c41a46551bbe0287911d30e5d4a8e1a0abb8c91fb2e3978`、英語。LRS3由来の公開デモサンプル。話者、撮影機材、照明、正解文は未確認。中間フレームの目視では、正面に近い単独話者の顔と発話中の口形を確認した。
- 前処理：配布されたground-truth動画から音声ストリームを除去。映像は元から224×224、25 fps、145フレーム、5.8秒。上流実装が全区間から16フレームを等間隔抽出。追加crop、resize、landmark、画素正規化は行われない。
- 実行コマンドと作業ディレクトリ：下記参照。作業ディレクトリは `research/vsr/vallr/VALLR`。

```bash
/usr/bin/time -v env MPLCONFIGDIR=/tmp/vallr-matplotlib-cache \
  /home/kosaki/.virtualenvs/vallr-vsr/bin/python main.py \
  --mode infer \
  --version V1 \
  --save_model_path checkpoints/VALLR.path \
  --videos_root outputs/20260908-lrs3-smoke/input/NqOjj1FCcVY_00007_gt_silent.mp4
```

- 実出力の種類：未後処理の音素クラス列。文章、信頼度は出力されない。
- 実出力の要約・期待した内容との違い：`[<pad>, <pad>, P, <pad>, Z, <pad>, <pad>, <pad>]`。標準CTCのblank除去を仮に行うと `P Z` だが、これは独自後処理を適用したモデル出力ではない。正解文が未確認なので認識の正否は評価していない。READMEが説明する英語文への第二段階は公開推論経路から呼ばれない。
- 入力動画長・処理時間：5.8秒。プロセス起動、import、動画16フレーム抽出、重みロード、CPU推論、出力を含む壁時計9.32秒。最大RSS 1,915,432 KiB。cold/warm分離、前処理単体、p50/p95は未測定。
- エラー・修正・残課題：`Models/ML_VALLR.py`の公開クラス名と`main.py`のimport名が不一致だったためalias importへ修正。`pronouncing==0.2.0`用に`setuptools==80.9.0`を固定。READMEの`--model_path`/`--infer_video_path`は実装に存在せず、実際の`--save_model_path`/`--videos_root`を使用。`uv pip check`はdecord wheelのplatform判定を1件報告するが、decordのimport、動画読込、推論は成功した。GPU推論、V2、文章復元、prompt、正解文との比較は未確認。

## 所感

- 導入のしやすさ：中程度。依存はuvで固定できたが、上流import不整合とsetuptools互換問題への対処が必要だった。共有NFS上ではuvのwheel検証が不安定だったため、環境はローカルファイルシステムの`~/.virtualenvs/vallr-vsr`に置いた。
- 認識内容の印象と根拠となる入力例：推論経路の疎通はできたが、5.8秒の英語発話に対し有効クラスは `P` と `Z` のみで、文章として利用できない。正解文未確認のため精度評価ではない。
- 待ち時間・撮影条件の制約：CPUでもcold相当9.32秒で終了した。入力は224×224・16フレーム以上が必要で、上流推論関数自体はresizeや口領域cropをしない。
- 次も試したいか、何を確かめたいか：正解文付きの同意済み英語動画を同じ前処理で入力し、標準CTC decodeの適用前後を分けて確認する。その後、公開された文章復元モデル・重み・tokenizerの有無を確認する。
- 採用候補 / 保留 / 不採用と理由：保留。V1重みのロードと音素クラス推論は成功したが、文章化、prompt、認識精度が未確認で、現状の出力だけではVSRとして利用判断できない。

## 比較を進める場合

- 共通評価セットID・件数・話者分割・正規化規則：未実施。
- VSR：WER / CER / 文完全一致 / 無結果率：未測定。
- cold start / warm p50・p95 / ピークVRAM：未測定。参考としてcold相当の全処理9.32秒、VRAM未測定。
- 前処理 / 推論 / 後処理の計測範囲：分離未測定。
- 文脈なし / 正しい文脈 / 誤った文脈の比較：prompt入力口がなく未実施。
- 再現確認者・再現結果：未確認。
- Codexで生成/修正した箇所、人が判断した点：uv環境定義、alias import、安全なstate-dictロード、試し実行、記録をCodexが実施。モデル採否と正解判定は人の判断が必要。

