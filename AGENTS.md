# 開発エージェント向け指示

## 目的と読む順序

現在は英語VSR（読唇）と手話モデルの選定・試運転を行う。
`README.md`、`docs/01-project-plan.md`、`docs/02-architecture.md`、`docs/04-model-research.md` を読む。
担当候補の `research/` 内のREADMEも読む。共同作業は `docs/06-team-workflow.md` を参照。
ユーザーの最新指示が最優先。当初の仕様書にあるUIや広いMust要件を現在の実装条件にしない。

## 実装の約束

- ドキュメント・報告は日本語、識別子は英語。入力言語と出力言語を区別する。
- VSRは `research/vsr/<model_id>/`、手話は `research/sign/<model_id>/` に検証を集約する。
- まず上流の推論を動画1本で再現し、実行コマンド・出力・所感を残す。共通APIや大規模評価の実装を先行させない。
- モデルごとに環境を分離。上流revision・重みSHA256・前処理・依存バージョンを記録する。
- 上流checkoutは各モデルの `upstream/`、重みは `weights/`、実行出力は `outputs/` に置きGit対象外にする。
- 自作の実行補助、設定、再現手順、許可された匿名の結果要約はGit管理する。秘密や実データを設定に埋め込まない。
- 手話の言語を必ず明記し、汎用ジェスチャーを手話翻訳と扱わない。
- モデル出力、音素列、gloss、文章補正を区別する。信頼度の尺度を記録し、未提供の値を捏造しない。
- 撮影原本は確認用の音声付きで保存可。VSR入力には音声トラックを除いた動画を使う。動画・顔ランドマーク・重み・秘密情報はGitへ追加しない。
- 未実行のコマンドや未測定の精度・遅延を検証済みと記載しない。不明点は未確認として残す。
- モデルの比較には同じ入力を使う。実行成否、認識内容の正しさ、主観的所感を別々に記録する。

## 将来の拡張

UI/APIは必要になった時点で別ディレクトリへ追加する。
`research/` はUI、Webフレームワーク、部屋管理などに依存させない。
アプリ追加のために検証ディレクトリを移動したり、単体実行をアプリ経由に置き換えたりしない。
共通化やサービス化は実際のモデル入出力を確認してから行う。

## 共同作業・検証

他の人の変更を上書きしない。モデル別に変更を分け、関連する確認と残課題を報告する。
既存モデルの取得・推論環境の整備・単体推論を現在の作業対象とする。
大規模学習、公開サービス化、外部へのデータ配信へ勝手に拡大しない。
