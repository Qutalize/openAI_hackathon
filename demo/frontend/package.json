{
  "name": "kotoba-link-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run prepare:workers && vite --host 127.0.0.1",
    "build": "tsc -b && npm run prepare:workers && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test:e2e": "playwright test",
    "test:unit": "node --test tests/speechOutput.test.mjs",
    "prepare:wasm": "node scripts/copy-wasm.mjs",
    "prepare:workers": "node scripts/build-worker.mjs"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.32",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.1",
    "@types/node": "^22.15.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.4.0",
    "esbuild": "^0.25.0",
    "prettier": "^3.5.3",
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  }
}
